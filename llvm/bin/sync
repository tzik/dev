#!/bin/bash
set -ex
cd "$(dirname "$0")/.."

cd src
shopt -s nullglob
for i in *; do
  (cd "$i"; git reset --hard; git pull --rebase)
done

cd chromium-tools
patch -p1 < ../../etc/patch/chromium-tools-fix.patch
